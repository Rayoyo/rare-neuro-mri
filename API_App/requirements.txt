# --- Core ML ---
torch>=2.2.0
torchvision>=0.17.0
numpy>=1.26.0,<2.3.0
pandas>=2.0.0
scikit-learn>=1.4.0
matplotlib>=3.7.0
seaborn>=0.12.0
Pillow>=10.2.0
tqdm>=4.65.0

# --- Data augmentation MRI ---
# imported by src/dataset.py: without these the package is not installable as-is
torchio>=0.19.0
SimpleITK>=2.3.0      # backend I/O required by TorchIO

# --- Dataset download ---
kaggle>=1.6.0         # used by the notebooks to pull the curated dataset

# --- API & Web ---
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
python-multipart>=0.0.9

# --- Utilities ---
requests>=2.31.0

# Note: Grad-CAM is implemented directly in API_App/gradcam.py with forward/backward
# hooks, so no extra explainability dependency is required.
